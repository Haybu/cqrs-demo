server:
  servlet:
    context-path: /pikes/read

spring:
  cloud:
    stream:
      bindings:
        eventsIn:
          destination: pike-events
          consumer.header-mode: raw
          group: pike-events
          content-type: application/json

